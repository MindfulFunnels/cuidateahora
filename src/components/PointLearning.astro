---
const { title, description, color } = Astro.props;
// Ejemplo: array de clases de color para el gradiente
---

<div
  class=`relative flex flex-col items-start gap-4 p-6 text-white ease-linear shadow-lg bg-gradient-to-br to-[#e8c39e] rounded-xl ${color}`
>
  <!-- Línea decorativa -->
  <h3 class='text-lg leading-snug sm:text-xl md:text-2xl lg:text-3xl'>
    {title}
  </h3>
  {
    description && (
      <p class='text-base leading-relaxed md:text-xl lg:text-2xl'>
        {description}
      </p>
    )
  }
</div>
